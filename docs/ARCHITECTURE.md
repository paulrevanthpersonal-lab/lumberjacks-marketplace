# Architecture

The application uses a small state controller for view routing, filtering, sorting, selected-product state, and a persistent cart. Pure collection and pricing rules live in `core.js` and are independently tested. The product gallery uses original CSS forms so the repository is self-contained and carries no uncertain stock-image licensing.
